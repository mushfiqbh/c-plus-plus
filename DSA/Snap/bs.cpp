#include <iostream>
#include <vector>
#include <cmath>
#include <algorithm>
using namespace std;

int main()
{
    int arr[10] = {4, 13, 24, 37, 44, 54, 68, 77, 89, 93};
    int sz = sizeof arr / sizeof(int);

    int low = 0, high = sz - 1, item = 1, mid, flag = 0;

    BinarySearch(){}

    cout << (flag ? "Item Found" : "Not Found");

    return 0;
}